import { HalTreeActionDirective } from './hal-tree-action.directive';

describe('HalTreeActionDirective', () => {
  it('should create an instance', () => {
    const directive = new HalTreeActionDirective();
    expect(directive).toBeTruthy();
  });
});
